# ========================================
# SECURITY WARNING: NEVER COMMIT .env FILE!
# ========================================
# This is a template file. Copy to .env and fill with your actual credentials.
# .env file is in .gitignore and should NEVER be pushed to GitHub.
# For production, use environment variables in your hosting platform (Render, Heroku, etc.)

# Database Configuration
# Use MongoDB Atlas for production: mongodb+srv://username:password@cluster.mongodb.net/dbname
MONGODB_URI=mongodb://localhost:27017/elearning

# JWT Configuration
# Generate strong secret: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_jwt_secret_key_here_change_in_production_use_long_random_string
JWT_EXPIRES_IN=7d

# Email Configuration (SMTP)
# IMPORTANT: Never commit real SMTP credentials!
# For Gmail: Use App Password (not your account password)
SMTP_HOST=smtp.mailtrap.io
SMTP_PORT=2525
SMTP_USER=your_mailtrap_user
SMTP_PASS=your_mailtrap_password
SMTP_FROM_EMAIL=noreply@elearning.com

# For production with Gmail:
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_email@gmail.com
# SMTP_PASS=your_app_password

# For production with SendGrid:
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASS=your_sendgrid_api_key
